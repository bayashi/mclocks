name: build-test

on:
  push:
    branches:
      - '*'
    tags-ignore:
      - '*'
    paths-ignore:
      - '**/*.md'
      - '**/.gitignore'
      - '**/dependabot.yml'
      - 'src-mcp/**'
      - 'web-assets/**/*.css'
      - '**/*.html'
  pull_request:
    branches:
      - '*'
    paths-ignore:
      - '**/*.md'
      - '**/.gitignore'
      - '**/dependabot.yml'
      - 'src-mcp/**'
      - 'web-assets/**/*.css'
      - '**/*.html'

jobs:
  setup:
    # On pull_request events, run jobs only for head branches starting with 'dependabot/' (push events run as usual).
    if: github.event_name != 'pull_request' || startsWith(github.head_ref, 'dependabot/')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: verify LF line endings
        shell: bash
        run: |
          set -euo pipefail
          crlf_lines="$(git ls-files --eol | awk '$2 == "w/crlf" { print }')"
          if [[ -n "${crlf_lines}" ]]; then
            echo "CRLF detected in tracked files:"
            echo "${crlf_lines}"
            exit 1
          fi

      - uses: ./.github/actions/_setup-rust-cache

      - uses: ./.github/actions/_setup-node-pnpm-cache

      - name: install frontend dependencies
        run: |
          pnpm install

  tauri-build:
    # On pull_request events, run jobs only for head branches starting with 'dependabot/' (push events run as usual).
    if: github.event_name != 'pull_request' || startsWith(github.head_ref, 'dependabot/')
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: ./.github/actions/setup-linux-tauri-rust

      - uses: ./.github/actions/_setup-node-pnpm-cache

      - uses: ./.github/actions/_install-pnpm-modules

      - name: display version info
        run: |
          pnpm tauri info

      # Non-main
      - name: Vite production build
        if: github.event_name != 'push' || github.ref != 'refs/heads/main'
        run: |
          pnpm build
          cargo check --manifest-path src-tauri/Cargo.toml

      # main push: full release build
      - name: Tauri build
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: pnpm tauri build

  # Frontend changes are expected to have been manually verified in a running app.
  # E2E tests run only on pull requests that have the required label, and on pushes to main after merge.
  lint-front:
    # On pull_request events, run jobs only for head branches starting with 'dependabot/' (push events run as usual).
    if: github.event_name != 'pull_request' || startsWith(github.head_ref, 'dependabot/')
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: ./.github/actions/_setup-node-pnpm-cache

      - uses: ./.github/actions/_install-pnpm-modules

      - name: run ESLint
        run: |
          pnpm exec eslint .

      - name: verify JS formatting (eslint --fix produces no diff)
        run: |
          pnpm exec eslint --fix "src/**/*.js" "*.config.js" "src-mcp/**/*.js"
          git diff --exit-code

  lint-and-test-server:
    # On pull_request events, run jobs only for head branches starting with 'dependabot/' (push events run as usual).
    if: github.event_name != 'pull_request' || startsWith(github.head_ref, 'dependabot/')
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: ./.github/actions/setup-linux-tauri-rust

      - name: verify Rust formatting
        run: |
          cargo fmt --manifest-path src-tauri/Cargo.toml -- --check

      - name: run Rust tests
        run: |
          cd src-tauri
          cargo test
